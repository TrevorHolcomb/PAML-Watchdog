{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Queue Size Message Params Schema",
  "description": "The schema for validating queue size message params",
  "type": "object",

  "required": [ "params" ],
  "properties": {
    "params": {
      "type": "object",
      "required": [ "queueSize" ],
      "properties": {
        "queueSize": {
          "type": "integer",
          "minimum": 0
        }
      }
    }
  }
}