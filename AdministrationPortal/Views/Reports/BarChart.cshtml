@model AdministrationPortal.ViewModels.Reports.BarChartViewModel

@{
    var myChart = new Chart(width: 600, height: 400)
        .AddTitle(Model.Title)
        .AddSeries(
            xValue: Model.XAxis,
            yValues: Model.YAxis)
        .Write();
}
