@model IEnumerable<WatchdogDatabaseAccessLayer.Rule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RuleTrigger)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EscalationChainId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AlertTypeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RuleTrigger)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EscalationChainId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AlertTypeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
