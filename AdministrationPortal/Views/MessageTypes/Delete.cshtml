@model WatchdogDatabaseAccessLayer.Models.MessageType

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>
    @TempData["PageMessage"]
</h3>
<div>
    <h4>MessageType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            Parameters
        </dt>

        <dd>
            <ul>
                @foreach (var parameter in Model.MessageTypeParameterTypes)
                {
                    <li>
                        @parameter.Name, @parameter.Type
                    </li>
                }
            </ul>
        </dd>


    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default @TempData["MessageTypeInUse"]" @TempData["ButtonState"]/>
            
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
