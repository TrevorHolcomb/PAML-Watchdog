@model WatchdogDatabaseAccessLayer.Models.MessageType

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>MessageType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            Parameters
        </dt>

        <!--TODO: make it look better-->

        <dd>Type, Name, Required</dd>
        <dd>
            <ul>
                @foreach (var parameter in Model.MessageTypeParameterTypes)
                {
                    <li>
                        @parameter.Type, @parameter.Name, @parameter.Required
                    </li>
                }
            </ul>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Name }) |
    @Html.ActionLink("Back to List", "Index")
</p>
