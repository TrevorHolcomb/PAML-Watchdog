@model AdministrationPortal.ViewModels.RegisteredEngines.DeleteRegisteredEngineViewModel

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>@Model.PageHeaderMessage</h3>
<div>
    <h4>Engine</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Engine.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Engine.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default @Model.ButtonClass()" @Model.ButtonState()/> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
